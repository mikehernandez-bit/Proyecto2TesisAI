# === App ===
APP_NAME="TesisAI Gen"
APP_ENV="dev"

# === External services (optional) ===
# API that provides institutional formats (Formatoteca, etc.)
FORMAT_API_BASE_URL="https://example.com/api"
FORMAT_API_KEY=""

# n8n webhook (optional). If empty, the app will generate a demo DOCX locally.
N8N_WEBHOOK_URL=""
# If you use n8n, configure it to call back this endpoint when finished:
# POST http://localhost:8000/api/n8n/callback/{project_id}
